name: grapher_user_draw
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.0-264.0.dev <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  grapher:
    path: ../grapher
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: